{
  "name": "Postgres+plv8",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "containerEnv": {
    "POSTGRES_PASSWORD": "mysecretpassword",
    "POSTGRES_DB": "postgres"
  },
  "postStartCommand": "/usr/sbin/sshd && docker-entrypoint.sh postgres &"
}
